---
- name: zadanie2
  hosts: my_vms
  become: true

  tasks:
    - name: inst tuned
      ansible.builtin.package:
        name: tuned
        state: present

    - name: start tuned
      ansible.builtin.service:
        name: tuned
        state: started
        enabled: true
